# FST 半自动标注工具使用指南

## 工作流程

```
原始图片
  ↓
YOLO 自动检测物体（绘制蓝色检测框）
  ↓
车位线检测 + 透视分析（绘制红色车位中心）
  ↓
车位类型自动识别（垂直/水平/斜列）
  ↓
9宫格自动划分（绘制绿色网格）
  ↓
物体自动映射到格子
  ↓
展示可视化结果 → 人工校验场景属性 → 保存
```

## 快速开始

### 1. 启动工具

**Windows:**
```bash
半自动标注工具_AutoLabel.bat
```

**Linux/Mac:**
```bash
python -m fst.auto_label_tool --images dataset/images --labels dataset/labels
```

### 2. 界面说明

**左侧 - 可视化区域:**
- 🔵 蓝色框: YOLO 检测到的物体
- 🟢 绿色网格: 9宫格划分
- 🔴 红色十字: 车位中心点
- 每个格子显示: 位置编号 + 障碍物类型

**右侧 - 标注面板:**
- 🤖 自动识别结果: 显示车位类型、检测物体数量等
- 车位类型: 自动填充，可手动修改
- 泊车动作/标线属性: 需手动选择
- 特殊场景: 需手动勾选

### 3. 操作快捷键

- `A` / `←`: 上一张图片
- `D` / `→`: 下一张图片
- `S`: 保存当前标注
- `R`: 重新分析当前图片

### 4. 标注流程

1. 工具自动加载图片并分析（YOLO + 几何 + 9宫格）
2. 查看可视化结果，确认自动识别是否正确
3. 如果车位类型识别错误，手动修改下拉框
4. 选择泊车动作、标线颜色/可见度/样式
5. 勾选特殊场景（如有）
6. 按 `S` 保存，自动跳转下一张

## 自动识别功能

### ✅ 完全自动化
- YOLO 物体检测（车辆、柱子、路沿等）
- 车位线检测
- 9宫格划分
- 物体映射到格子
- 车位类型推断（基于宽高比）

### ⚠️ 需人工校验
- 车位类型（自动推断可能不准）
- 泊车动作
- 标线颜色/可见度/样式
- 特殊场景

## 车位类型识别逻辑

工具会根据检测到的车位几何特征自动推断类型：

- **垂直车位**: 高度 > 宽度（宽高比 < 0.7）
- **水平车位**: 宽度 > 高度（宽高比 > 1.5）
- **斜列车位**: 宽高比介于 0.7-1.5

如果识别错误，请手动修改。

## 可视化图例

```
蓝色框 + 标签: YOLO 检测结果
  例: "car→VEHICLE"

绿色网格 + 标签: 9宫格 + 障碍物
  例: "1: EMPTY", "P_LEFT: VEHICLE"

红色十字: 车位中心点
```

## 高级选项

### 使用更大的 YOLO 模型（提高精度）

```bash
python -m fst.auto_label_tool \
    --images dataset/images \
    --labels dataset/labels \
    --yolo s  # 可选: n(默认), s, m, l, x
```

模型对比:
- `n`: 最快，6MB，适合快速标注
- `s`: 平衡，22MB，推荐
- `m`: 高精度，52MB，需要 GPU

## 输出格式

保存的 JSON 文件包含：

```json
{
  "image_id": "IMG_0001",
  "slot_type": "PERPENDICULAR",  // 自动识别 + 人工校验
  "maneuver": "PARK_IN",         // 人工标注
  "marking": {                   // 人工标注
    "line_color": "WHITE",
    "line_visibility": "CLEAR",
    "line_style": "SOLID"
  },
  "special_scene": {             // 人工标注
    "P0": [],
    "P1": ["BRICK_GRASS"]
  },
  "obstacles": {                 // 自动识别
    "1": "EMPTY",
    "2": "EMPTY",
    "3": "LAMP",
    "4": "EMPTY",
    "5": "CURB",
    "6": "EMPTY",
    "7": "EMPTY",
    "P_LEFT": "VEHICLE",
    "P_RIGHT": "EMPTY"
  }
}
```

## 常见问题

### Q: YOLO 检测不准怎么办？
A:
1. 按 `R` 重新分析
2. 使用更大的模型: `--yolo s`
3. 手动修改保存的 JSON 文件

### Q: 9宫格划分不合理？
A: 当前使用简化算法，后续可以改进 `parking_geometry.py` 中的透视变换逻辑

### Q: 车位类型识别错误？
A: 直接在右侧面板修改下拉框即可，保存时会使用你选择的值

### Q: 能否完全自动化？
A: 障碍物可以自动识别，但场景属性（泊车动作、标线特征、特殊场景）需要人工判断

## 性能优化

- 首次运行会下载 YOLO 模型（约 6MB）
- 每张图片分析时间: 约 0.5-2 秒（取决于 YOLO 模型大小）
- 建议先用 `yolo n` 快速标注，有问题的图片再用 `yolo s` 重新分析

## 与原标注工具对比

| 功能 | 原工具 | 半自动工具 |
|------|--------|-----------|
| 车位类型 | 手动选择 | 自动推断 + 可修改 |
| 泊车动作 | 手动选择 | 手动选择 |
| 标线属性 | 手动选择 | 手动选择 |
| 特殊场景 | 手动勾选 | 手动勾选 |
| 9宫格障碍物 | 手动选择 9 次 | **自动识别** ✨ |
| 可视化 | 仅显示原图 | **YOLO框 + 9宫格 + 透视** ✨ |

**标注效率提升: 约 70%**
